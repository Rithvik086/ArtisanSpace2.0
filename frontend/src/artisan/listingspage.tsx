import React from 'react';
import Dashboard from './Dashboardpage';

// Listings page - for now reuse the Dashboard product section
export default function ListingsPage(): React.ReactElement {
  return (
    <div>
      <Dashboard />
    </div>
  );
}